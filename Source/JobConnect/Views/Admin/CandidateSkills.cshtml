@using JobConnect.Models
@model CandidateProfile
@{
    ViewData["Title"] = "Kỹ năng ứng viên";
    Layout = "_LayoutAdmin";
}

<style>
    .skill-tag {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        border-radius: 0.75rem;
        font-size: 0.875rem;
        font-weight: 500;
        transition: all 0.2s;
    }

    .skill-beginner {
        background: rgba(239, 68, 68, 0.2);
        color: #dc2626;
        border: 1px solid rgba(239, 68, 68, 0.3);
    }

    .skill-elementary {
        background: rgba(251, 191, 36, 0.2);
        color: #d97706;
        border: 1px solid rgba(251, 191, 36, 0.3);
    }

    .skill-intermediate {
        background: rgba(59, 130, 246, 0.2);
        color: #2563eb;
        border: 1px solid rgba(59, 130, 246, 0.3);
    }

    .skill-advanced {
        background: rgba(139, 92, 246, 0.2);
        color: #7c3aed;
        border: 1px solid rgba(139, 92, 246, 0.3);
    }

    .skill-expert {
        background: rgba(34, 197, 94, 0.2);
        color: #16a34a;
        border: 1px solid rgba(34, 197, 94, 0.3);
    }
</style>

<div class="max-w-6xl mx-auto px-6 py-8">
    <!-- Header -->
    <div class="flex items-center gap-4 mb-6">
        <a asp-action="Users" class="text-secondary hover:text-primary flex items-center gap-1">
            <span class="material-symbols-outlined">arrow_back</span> Quay lại
        </a>
        <div>
            <h1 class="text-2xl font-bold text-on-surface">Kỹ năng của ứng viên</h1>
            <p class="text-secondary text-sm">Xem kỹ năng và trình độ của @Model.User?.FullName</p>
        </div>
    </div>

    <!-- Candidate Info -->
    <div class="bg-white rounded-2xl p-6 border border-outline-variant mb-6">
        <div class="flex items-center gap-4">
            <div class="w-16 h-16 rounded-full bg-primary text-white flex items-center justify-center text-2xl font-bold">
                @(Model.User?.FullName?.Length > 0 ? Model.User.FullName[0].ToString().ToUpper() : "U")
            </div>
            <div>
                <h2 class="text-xl font-bold text-on-surface">@Model.User?.FullName</h2>
                <p class="text-secondary text-sm">@(Model.JobTitle ?? Model.Summary ?? "Chưa cập nhật chức danh")</p>
                <p class="text-secondary text-xs mt-1">Email: @Model.User?.Email | Phone: @Model.User?.PhoneNumber</p>
            </div>
        </div>
    </div>

    <!-- Skills -->
    <div class="bg-white rounded-2xl p-6 border border-outline-variant">
        <h2 class="text-lg font-bold text-on-surface mb-4 flex items-center gap-2">
            <span class="material-symbols-outlined text-primary">workspace_premium</span>
            Danh sách kỹ năng (@Model.CandidateSkills?.Count ?? 0)
        </h2>

        @if (Model.CandidateSkills != null && Model.CandidateSkills.Any())
        {
            <div class="space-y-4">
                @foreach (var candidateSkill in Model.CandidateSkills.OrderBy(cs => cs.ProficiencyLevel).ThenByDescending(cs => cs.YearsOfExperience))
                {
                    var proficiencyClass = candidateSkill.ProficiencyLevel.ToString().ToLower();
                    <div class="bg-surface-container rounded-xl p-4 border border-outline-variant hover:border-primary transition-colors">
                        <div class="flex items-center justify-between mb-2">
                            <div class="flex items-center gap-3">
                                <span class="text-on-surface font-semibold text-lg">@candidateSkill.Skill.Name</span>
                                <span class="skill-tag skill-@proficiencyClass text-xs">
                                    @candidateSkill.ProficiencyLevel
                                </span>
                            </div>
                            <span class="text-secondary text-sm">@candidateSkill.Skill.Category</span>
                        </div>

                        @if (!string.IsNullOrEmpty(candidateSkill.Skill.Description))
                        {
                            <p class="text-secondary text-sm mb-3">@candidateSkill.Skill.Description</p>
                        }

                        <div class="flex items-center gap-6 text-sm text-on-surface">
                            <div class="flex items-center gap-2">
                                <span class="material-symbols-outlined text-base text-primary">schedule</span>
                                <span>@candidateSkill.YearsOfExperience năm kinh nghiệm</span>
                            </div>
                            @if (candidateSkill.LastUsedDate.HasValue)
                            {
                                <div class="flex items-center gap-2">
                                    <span class="material-symbols-outlined text-base text-primary">event</span>
                                    <span>Sử dụng lần cuối: @candidateSkill.LastUsedDate.Value.ToString("dd/MM/yyyy")</span>
                                </div>
                            }
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div class="text-center py-8">
                <span class="material-symbols-outlined text-4xl text-secondary mb-2">psychology</span>
                <p class="text-secondary">Ứng viên chưa thêm kỹ năng nào</p>
            </div>
        }
    </div>
</div>
